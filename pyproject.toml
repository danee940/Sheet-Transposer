[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]

[tool.pytest.ini_options]
addopts = "--cov=transpose --cov-report=term-missing --cov-fail-under=95"
testpaths = ["test_transpose.py"]

[tool.coverage.run]
source = ["transpose"]

[tool.coverage.report]
exclude_also = ['if __name__ == "__main__":']
